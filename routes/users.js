const express = require('express');
const router = express.Router();

// Placeholder for user routes (if needed in future)
router.get('/', (req, res) => res.send('User route'));

module.exports = router;